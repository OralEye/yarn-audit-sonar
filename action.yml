# action.yml
name: 'Yarn Audit Sonar'
description: 'Run yarn audit and output the file in a valid format for SonarCloud'
outputs:
   status:
    description: "The Success/Failure of the action"
runs:
  using: 'docker'
  image: 'Dockerfile'
